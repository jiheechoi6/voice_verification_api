"""
Django settings for VoiceVerifier project.

Generated by 'django-admin startproject' using Django 3.2.6.

For more information on this file, see
https://docs.djangoproject.com/en/3.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/3.2/ref/settings/
"""

from pathlib import Path
import os
from datetime import datetime 

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# Threshold that is used to decide voice verification results
# If score is bigger than THRESHOLD, the user is verified and vice versa
THRESHOLD = 0.25

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/3.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-e+)j7#+5&jbwsp6krk2gb_z_=uy60yax9=-q)nypq)h3gcfqcu'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = ["172.19.0.3", '192.168.0.246', '175.197.109.183']

# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'VoiceVerifier',
    'drf_yasg',
    'corsheaders'
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'corsheaders.middleware.CorsMiddleware',
]

ROOT_URLCONF = 'VoiceVerifier.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'VoiceVerifier.wsgi.application'


# Database
# https://docs.djangoproject.com/en/3.2/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}


# Password validation
# https://docs.djangoproject.com/en/3.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/3.2/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_L10N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/3.2/howto/static-files/

STATIC_URL = '/static/'

STATIC_ROOT = os.path.join(BASE_DIR, "static/")

# Default primary key field type
# https://docs.djangoproject.com/en/3.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'


CORS_ALLOW_ALL_ORIGINS = True


# Logging
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters':{
        'timemessage': {
            'format': '{asctime} {message}',
            'style': '{',
        },
    },
    'handlers': {
        'utillog': {
            'level': 'DEBUG',
            'class': 'logging.FileHandler',
            'filename': '/home/VoiceVerifier/logs/system_log/'+datetime.today().strftime('%Y-%m-%d')+'.log',
            'formatter': 'timemessage',
        },
        # 'dblog': {
        #     'level': 'DEBUG',
        #     'class': 'logging.FileHandler',
        #     'filename': '/home/VoiceVerifier/logs/dblog.txt',
        #     'formatter': 'timemessage',
        # },
        'requestlog': {
            'level': 'DEBUG',
            'class': 'logging.FileHandler',
            'filename': '/home/VoiceVerifier/logs/request_log/'+datetime.today().strftime('%Y-%m-%d')+'.log',
            'formatter': 'timemessage',
        },
        'enrollverify': {
            'level': 'DEBUG',
            'class': 'logging.FileHandler',
            'filename': '/home/VoiceVerifier/logs/activity_log/'+datetime.today().strftime('%Y-%m-%d')+'.log',
            'formatter': 'timemessage',
        }
    },
    'loggers': {
        'django': {
            'handlers': ['utillog'],
            'level': 'DEBUG',
            'propagate': False,
        },
        'django.server': {
            'handlers': ['requestlog'],
            'level': 'DEBUG',
            'propagate': False,
        },
        'django.request': {
            'handlers': ['requestlog'],
            'level': 'DEBUG',
            'propagate': False,
        },
        'enrollverify': {
            'handlers': ['enrollverify'],
            'level': 'DEBUG',
            'propagate': False,
        },
    },
}
